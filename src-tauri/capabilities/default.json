{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "메인 창의 기본 권한. 단계 2에서 shell:allow-execute(ExifTool 사이드카)를 추가한다.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
